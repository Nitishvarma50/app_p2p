{
    "name": "p2p-file-transfer",
    "version": "1.0.0",
    "description": "Secure P2P File Transfer App",
    "main": "electron-main.js",
    "scripts": {
        "start": "electron .",
        "build": "vite build",
        "build:windows": "electron-builder --win",
        "build:ios": "npx cap add ios && npx cap open ios",
        "build:android": "npm run build && npx cap sync && npx cap open android",
        "deploy": "npx wrangler pages deploy dist --project-name p2p-file-transfer"
    },
    "keywords": [
        "p2p",
        "webrtc",
        "file-transfer"
    ],
    "author": "Your Name",
    "license": "MIT",
    "dependencies": {
        "@capacitor/android": "^5.0.0",
        "@capacitor/app": "^5.0.0",
        "@capacitor/core": "^5.0.0",
        "@capacitor/filesystem": "^5.0.0",
        "@capacitor/ios": "^5.0.0",
        "@capacitor/share": "^5.0.8",
        "@capawesome-team/capacitor-android-foreground-service": "^5.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^5.0.0",
        "electron": "^25.0.0",
        "electron-builder": "^24.0.0",
        "vite": "^4.0.0"
    }
}